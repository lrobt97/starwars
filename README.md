# Star wars app
This app uses swapi-php to get character information from the star wars API. The user needs to select 3 items then press download csv to recieve more information about those characters in csv format.

# Run instructions
Go to the root directory in the file explorer and open command prompt. Type the command php spark serve and run the localhost on a web browser
